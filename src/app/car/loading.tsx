import React from 'react';

const Loading = () => {
    return (
        <div className={'container'}>
            <h2 className={'title'}>Loading...</h2>
        </div>
    );
};

export default Loading;